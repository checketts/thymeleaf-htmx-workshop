{
  "name": "pas",
  "version": "1.0.0",
  "description": "In order to open the application and have the Oauth redirect url match you need to map  pas-local.churchofjesuschrist.org to your local host in /etc/hosts",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "npx tailwindcss -i ./src/main/resources/templates/app.src.css -o ./src/main/resources/static/css/output.css --watch",
    "build": "npx tailwindcss -i ./src/main/resources/templates/app.src.css -o ./src/main/resources/static/css/output.css --minify"
  },
  "repository": {
    "type": "git",
    "url": "git@ssh.dev.azure.com:v3/churchofjesuschrist/GA/pas"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "postcss": "^8.4.18",
    "tailwindcss": "^3.2.4"
  },
  "dependencies": {
  }
}
